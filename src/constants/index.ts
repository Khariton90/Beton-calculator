export enum AppRoute {
  Main = '/',
  Login = '/login',
  BetonFactory = '/beton-factory',
  NotFound = '*'
}

export enum AuthorizationStatus {
  Auth = 'AUTH',
  NoAuth = 'NO_AUTH',
  Unknown = 'UNKNOWN'
}